<Page
    x:Class="TwinSpool.Views.SyncRunPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.Resources>
        <Style x:Key="StretchListViewItemStyle" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Padding" Value="0" />
        </Style>
    </Page.Resources>
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="Sync" Style="{StaticResource SectionHeaderTextStyle}" />
            <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.4*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0"
                          ItemsSource="{Binding Profiles}"
                          SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                          DisplayMemberPath="Name"
                          SelectionChanged="ProfileSelection_Changed"
                          MinHeight="40"
                          Margin="0,0,18,0" />
                <Button Grid.Column="1" Click="RefreshPreview_Click" IsEnabled="{Binding CanRefreshPreview}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Preview" />
                        <TextBlock Text="{Binding RefreshPreviewButtonText}" Foreground="White" />
                    </StackPanel>
                </Button>
                <Button Grid.Column="2" Click="Start_Click" IsEnabled="{Binding CanStartSync}" Margin="16,0,0,0">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <SymbolIcon Symbol="Switch" />
                        <TextBlock Text="{Binding StartSyncButtonText}" Foreground="White" />
                    </StackPanel>
                </Button>
            </Grid>
            <TextBlock Text="{Binding Status}" Style="{StaticResource BodyTextStyle}" Margin="0,10,0,0" />
            <TextBlock Text="{Binding CurrentFile}" Style="{StaticResource BodyTextStyle}" Margin="0,6,0,0" TextWrapping="WrapWholeWords" />
            <ProgressBar Value="{Binding TotalProgressPercent}" Maximum="100" Height="12" Margin="0,14,0,0" />
            <TextBlock Text="{Binding TotalProgressText}" Style="{StaticResource BodyTextStyle}" Margin="0,6,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="18" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Margin="0,0,0,8">
                    <TextBlock Text="Source" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource AppMutedBrush}" />
                    <TextBlock Text="{Binding SourceRootDisplay}" Style="{StaticResource BodyTextStyle}" Margin="0,8,0,0" />
                </StackPanel>
                <ListView Grid.Row="1" ItemsSource="{Binding PlannedFiles}" ItemContainerStyle="{StaticResource StretchListViewItemStyle}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12" Opacity="{Binding ItemOpacity}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <FontIcon Glyph="&#xE72A;" FontSize="16" Foreground="{StaticResource AppAccentBrush}" VerticalAlignment="Top" Margin="0,1,10,0" Opacity="{Binding ActionIconOpacity}" />
                                    <TextBlock Grid.Column="1" Text="{Binding RelativePath}" Foreground="White" TextWrapping="WrapWholeWords" />
                                </Grid>
                                <ProgressBar Grid.Row="1" Value="{Binding ProgressPercent}" Maximum="100" Height="8" Margin="0,8,0,0" />
                                <TextBlock Grid.Row="2" Text="{Binding ProgressText}" Style="{StaticResource BodyTextStyle}" Margin="0,4,0,0" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>

            <Rectangle Grid.Column="1" Fill="{StaticResource AppInputBorderBrush}" Width="1" HorizontalAlignment="Center" />

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Margin="0,0,0,8">
                    <TextBlock Text="Destination" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource AppMutedBrush}" />
                    <TextBlock Text="{Binding DestinationRootDisplay}" Style="{StaticResource BodyTextStyle}" Margin="0,8,0,0" />
                </StackPanel>
                <ListView Grid.Row="1" ItemsSource="{Binding DestinationFiles}" ItemContainerStyle="{StaticResource StretchListViewItemStyle}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,12" Opacity="{Binding ItemOpacity}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <FontIcon Glyph="&#xE710;" FontSize="16" Foreground="{StaticResource AppAccentBrush}" VerticalAlignment="Top" Margin="0,1,10,0" Opacity="{Binding ActionIconOpacity}" />
                                <TextBlock Grid.Column="1" Text="{Binding RelativePath}" Foreground="White" TextWrapping="WrapWholeWords" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
    </Grid>
</Page>

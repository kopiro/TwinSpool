<Page
    x:Class="XboxRemoteSync.Views.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Saved Profiles" Style="{StaticResource SectionHeaderTextStyle}" />
        </StackPanel>

        <ListView x:Name="ProfilesList"
                  Grid.Row="1"
                  Margin="0,18,0,18"
                  ItemsSource="{Binding Profiles}"
                  SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                  IsItemClickEnabled="True"
                  ItemClick="ProfilesList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,0,0,12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="SemiBold" Foreground="White" />
                            <TextBlock Text="{Binding ConnectionDisplay}" Style="{StaticResource BodyTextStyle}" Margin="0,6,0,0" />
                            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,6,0,0">
                                <FontIcon Glyph="&#xE72A;" FontSize="16" Foreground="{StaticResource AppAccentBrush}" />
                                <TextBlock Text="{Binding DestinationSummary}" Style="{StaticResource BodyTextStyle}" />
                            </StackPanel>
                            <TextBlock Text="{Binding LastSummary}" Style="{StaticResource BodyTextStyle}" Margin="0,4,0,0" />
                        </StackPanel>
                        <Rectangle Grid.Row="3" Height="1" Fill="{StaticResource AppInputBorderBrush}" Margin="0,14,0,0" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <Button Click="Add_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Add" />
                    <TextBlock Text="Add Profile" Foreground="White" />
                </StackPanel>
            </Button>
            <Button Click="Edit_Click" IsEnabled="{Binding HasSelectedProfile}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Edit" />
                    <TextBlock Text="Edit" Foreground="White" />
                </StackPanel>
            </Button>
            <Button Click="Delete_Click" IsEnabled="{Binding HasSelectedProfile}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <SymbolIcon Symbol="Delete" />
                    <TextBlock Text="Delete" Foreground="White" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</Page>
